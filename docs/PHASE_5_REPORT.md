# Фаза 5: Обновление документации и README

> Коммит: `cadc42c`
> Дата: 2026-04-10
> Статус: ✅ Завершена

## Цель

Привести документацию в соответствие с реальным состоянием проекта после Фаз 0–4 рефакторинга. Убрать все упоминания удалённого кода (firecrawl, firmsru, pipeline_runs) и обновить устаревшие данные (количество тестов, структура файлов, требования).

## Изменения

### 5.1 README.md (22 правки)

**Удаления:**
- Убраны все упоминания `firecrawl` (16 мест): из описания пайплайна, структуры файлов, секции установки, требований, troubleshooting, конфигурации
- Убраны упоминания `firmsru` (3 места): из списка источников, структуры scrapers, пайплайна
- Убрано упоминание `pipeline_runs` (1 место): из таблицы БД
- Удалена секция «Firecrawl CLI не установлен» из Troubleshooting
- Удалена инструкция установки Firecrawl CLI из секции установки и разработки
- Убрано требование `Node.js 18+` (не нужен без Firecrawl)
- Убран `FIRECRAWL_API_KEY` из списка поддерживаемых переменных окружения
- Удалена строка `raw_companies.merged_into → ⚠️ DEPRECATED` из схемы связей

**Обновления:**
- `firecrawl` → `web_search (DuckDuckGo)` во всех описаниях пайплайна и конфигурации
- Количество тестов: `169` → `240`
- В дерево структуры добавлен `web_search.py # DuckDuckGo web search`
- В модулях scrapers: `firecrawl...` → `web_search, dgis...`

### 5.2 DATABASE_GUIDE.md (9 правок)

- Удалена модель `PipelineRun` из диаграммы архитектуры
- Обновлены source values: `firecrawl, firmsru` → `web_search`
- Обновлено описание website: `найден через firecrawl` → `найден через web_search`
- **Удалён весь раздел 2.4** `pipeline_runs` (DEPRECATED) — заголовок, предупреждение, таблица колонок, индекс
- Удалён блок `pipeline_runs` из ER-диаграммы
- Переименованы разделы: 2.5 → 2.4, 2.6 → 2.5
- В диаграмме потока данных: `firecrawl` → `web_search`
- В примерах кода: `source="firecrawl"` → `source="web_search"` (2 места)

### 5.3 granite-crm-foundation-guide.md (4 правки)

- Удалена строка `pipeline_runs | PipelineRunRow` из таблицы схемы БД
- Удалена строка `pipeline_runs — нет FK` из раздела связей
- Обновлено описание ORM drift: убраны `pipeline_runs и`
- Обновлено описание проблемы синхронизации: убраны `pipeline_runs и`

### 5.4 granite-crm-audit-guide.md (6 правок)

- Контрольно-точки: `pipeline_runs` → `подсчёт записей в таблицах`
- SSRF-секция: `firecrawl.py` → пометка «удалённые модули»
- Data loss: `firecrawl_client.py` → пометка «модуль удалён»
- ORM drift: убраны «отсутствующие таблицы (pipeline_runs)»
- Таблица pipeline/: `firecrawl_client.py` → `web_client.py`
- Таблица scrapers/: `firecrawl.py` → `web_search.py`

### 5.5 Прочие

- **PHASE_2_REPORT.md** — обновлена ссылка на Фазу 5 (документация очищена)
- **temp_pr.md** — удалён мусорный файл (2 строки, не являлся частью проекта)

## Результаты проверки

```bash
# Grep на устаревшие упоминания в md/yaml файлах
grep -ri "firecrawl|firmsru|pipeline_runs" --include="*.md" --include="*.yaml" -l
# Результат: только PHASE_2_REPORT.md (исторический контекст)
#           и audit-guide.md (контекст "удалённый модуль", "заменил firecrawl")

# Все 240 тестов проходят
python -m pytest tests/ -v
# ======================== 240 passed ========================
```

## Критерии успеха (из REFACTORING_PLAN.md)

- [x] Документация соответствует реальному коду
- [x] Нет упоминаний удалённого кода в активной документации
  (допускаются исторические ссылки в отчётах фаз 0–4)
- [x] Новые разработчики могут понять проект по README

## Изменённые файлы (6)

| Файл | Изменения |
|------|-----------|
| `README.md` | -86 +32 строк: убран firecrawl/firmsru/pipeline_runs, обновлены структура, пайплайн, тесты |
| `docs/DATABASE_GUIDE.md` | -45 +9 строк: удалён раздел pipeline_runs, обновлены схема, ER-диаграмма, примеры |
| `docs/granite-crm-foundation-guide.md` | -6 +2 строки: убраны pipeline_runs, PipelineRunRow |
| `docs/granite-crm-audit-guide.md` | -6 +6 строк: обновлены таблицы модулей, контекст удалённых модулей |
| `docs/PHASE_2_REPORT.md` | -1 +1 строка: обновлена ссылка на Фазу 5 |
| `temp_pr.md` | Удалён |

## Итог рефакторинга (Фазы 0–5)

| Фаза | Коммит | Описание |
|------|--------|----------|
| 0 | `044be35` | Удаление мёртвого кода |
| 1 | `1060f33` | Исправление code smells |
| 2 | `165fc13` | Централизация конфигурации |
| 3 | `cd8c69a` | Улучшение error handling и логирования |
| 4 | `d605ed2` | Оптимизация производительности обогащения |
| 5 | `cadc42c` | Обновление документации и README |
